version: "3.9"

services:

  api:
    build: .
    container_name: prompt_guardrails_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: redis:7
    container_name: prompt_guardrails_redis
    ports:
      - "6379:6379"
